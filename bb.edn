{:paths ["src" "test"]
 :tasks
 {test {:doc "Run tsukuroi murakumo actor boundary tests"
        :task (do
                (require 'clojure.test
                         'tsukuroi.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'tsukuroi.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
